{
  "name": "argonom",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:ci": "ASTRO_TELEMETRY_DISABLED=1 astro build",
    "build:vercel": "npm run check:registry && npm run build:ci",
    "build:hosting": "npm run check:registry && npm run build:ci",
    "check:registry": "tsx scripts/check-registry.ts",
    "pages:deploy": "npm run build:ci && wrangler pages deploy dist --project-name=argonom-com-ua --branch=main",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.1.5"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "wrangler": "^4.81.1"
  }
}
